<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shaafi.blooddonate">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/main_screen"/>

        <activity android:name=".StartPic"
            android:theme="@android:style/Theme.Wallpaper.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterClass" />
        <activity android:name=".PhoneVerify" />
        <activity android:name=".SearchPage" />
        <activity android:name=".SearchResultList" />
        <activity android:name=".VerificationCodeInput" />
        <activity android:name=".MemberInfoDisplay" />
        <activity android:name=".AboutUs" />
        <activity android:name=".UserInfo" />
        <activity android:name=".UserPhoneNumerInput"/>
    </application>

</manifest>
