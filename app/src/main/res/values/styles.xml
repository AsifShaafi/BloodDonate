<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
    </style>

    <style name="start_screen" parent="android:Theme.Wallpaper.NoTitleBar.Fullscreen">
    </style>

    <style name="blue_bg" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/blue_color</item>
        <item name="android:background">@color/blue_color</item>
    </style>

    <style name="main_screen" parent="AppTheme">
        <item name="android:windowBackground">@color/home</item>
    </style>

</resources>
